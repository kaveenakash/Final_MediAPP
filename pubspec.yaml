name: medicine_remainder_app
description: A new Flutter project.


publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0" 

dependencies:
  flutter:
    sdk: flutter
 
  
  


  cupertino_icons: ^1.0.2 
  flutter_svg: ^1.0.3
  lottie: ^1.2.2
  get_storage:
  get:
  intl:
  google_fonts:
  date_picker_timeline: 
  # flutter_local_notifications: ^9.4.0
  flutter_native_timezone:
  sqflite:
  flutter_staggered_animations:

dev_dependencies:
  flutter_test:
    sdk: flutter 
  flutter_launcher_icons: ^0.9.2

  flutter_lints: ^1.0.0

flutter_icons:
  image_path:"asstes/launch.png"
  android:true

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/icons/

